@{
    ViewData["Title"] = "Lista Przejœæ";
}

<h1>Lista zdefiniowanych przejœæ</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Dodaj nowe przejœcie</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>Sk¹d (Lokalizacja)</th>
            <th>Dok¹d (Lokalizacja)</th>
            <th>Kierunek (Ikona)</th>
            <th>Pozycja X / Y</th>
            <th>Akcje</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@(item.SourceLocation?.Name ?? "Brak")</td>
                <td>@(item.TargetLocation?.Name ?? "Brak")</td>
                <td>@item.Direction</td>
                <td>@item.PositionX% / @item.PositionY%</td>
                <td>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Usuñ</a>
                </td>
            </tr>
        }
    </tbody>
</table>